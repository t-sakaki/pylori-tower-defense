{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "out" }
    }
  ],
  "installCommand": "corepack enable pnpm && pnpm install",
  "buildCommand": "pnpm run build"
}